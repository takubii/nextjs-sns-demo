'use client';

import { Dispatch, SetStateAction } from 'react';

export default function SignUpForm(props: { showModal: Dispatch<SetStateAction<boolean>> }) {
  return (
    <div>
      {/* TODO
       */}
    </div>
  );
}
