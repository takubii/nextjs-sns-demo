import { Dispatch, SetStateAction } from 'react';

export default function SignInForm(props: { showModal: Dispatch<SetStateAction<boolean>> }) {
  const { showModal } = props;

  return (
    <div>
      {/* TODO
       */}
    </div>
  );
}
